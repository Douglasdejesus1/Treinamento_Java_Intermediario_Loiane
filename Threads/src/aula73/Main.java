package aula73;

public class Main {
	/*Pode ser útil suspender uma thread. Por exemplo, uma thread que mostra a hora do dia.
	 * Podemos suspender e posteriormente resumir a execução;
	 * 
	 * Até o java 2 existiam o suspend, resume e stop
	 * o suspend podia causar deadlock, o resulme tbm foi retirado pois nao funciona sem o suspend
	 * O stop foi substituido pelo método interrupt
	 * 
	 * Mas ainda podemos adicionar esses comportamentos na nossa Thread de maneira mais segura
	 * 
	 * suspend suspende temporiamente a execucao da Thread
	 * resume: resume a execução da Thread
	 * stop: temina a execucao da Thread
	 */
	
	
	

}
