package aula67;

public class Main {

	/*Multitarefas: processos e threads
	 * Processo é um programa que está sendo executado
	 * Thread é a menor unidade de código que pode ser executado
	 *   ou seja, permite ao programa executar 2 ou mais tarefas ao mesmo tempo
	 * Exemplo: editor de texto: formatar e imprimir ao mesmo tempo
	 */
	/*Ciclo de vida
	 * New: new Thread()
	 * Runnable: start() ----notify()/notifyAll()/resume()
	 * Waiting: sleep()/wait()/suspend()
	 * Running run()/yield()
	 * Dead
	 * 
	 */
	/*start: inicia
	 * run: executa a tarefa da thread
	 * sleep: coloca a thread para dormir por x millisegundos
	 * 
	 */
}
